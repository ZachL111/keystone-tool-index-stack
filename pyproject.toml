[project]
name = "keystone-tool-index-stack"
version = "0.1.0"
description = "Package a Python local lab for index analysis with safe and unsafe fixtures, remediation hints, and documented operating limits."
requires-python = ">=3.11"
